# commande GIT pour cloner un repository :
clone https://github.com/roger-latiere/git-begins

# les 3 commandes push dans l'ordre (les deux premières amènent à "push" :
git add nom_de_fichier
git commit -m "détailler la signification de l'ajout/modification"
git push origin main

# commande GIT pour "pull":
git pull

# commandes pour créer une branche et s'y déplacer:
git branch nom_de_la_branche
git checkout nom_de_la_branche
